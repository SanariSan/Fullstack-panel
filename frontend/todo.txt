data models for api
base url
urls for api
json validation



датакласс на вход в апи
-инициализируется в контейнере
-на вход имеет строгий тип + объект в конструкторе
-внутри себя ничего не кастит и не плейсхолдерит, нужен только для контроля компонента на передачу четкого формата данных

датакласс на выход из апи
-инициализируется в саге, после проверки на неуспешный запрос + после .json() парса
-на вход имеет НЕстрогий формат (any сущность)
-внутри осуществляет всевозможную валидацию, плейсхолдинг, тайп чекинг и кастинг

условие для датакласса на выход - error ответ апи должен попадать в error раздел после вызова .safe()
это надо проверить!
если ошибочный код ответа не определяет его в error раздел - варианта х?:
 ?-проверка кода ответа + переопределение в нужную категорию
 ?-универсиализация датакласса, путем попытки двойного приведения (ошибка() ИЛИ успех())
 ?-

основная проблема - абстрагировать всевозможные проверки ответа из саги в отдельный модуль
то есть сагу не должен волновать код ответа или наличие каких-то полей, чтобы исходя из этого переопределить ответ и тд
это должно выполняться вне саги

!!!итого - понять что конкретно попадает в error, что в response